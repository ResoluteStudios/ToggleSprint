{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"${authors}"
	],
	"contact": {
		"homepage": "${homepage}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "assets/${id}/icon.png",
	"environment": "${environment}",
	"entrypoints": {
		"client": [
			"resolutestudios.togglesprint.Togglesprint"
		]
	},
	"mixins": [
		"${id}.mixins.json"
	],
	"depends": {
		"fabricloader": ">=0.18.6",
		"minecraft": ">=1.20.1",
		"java": ">=17",
		"fabric-api": "*"
	}
}